# URL base de la API (sin barra final).
# El login es POST {VITE_API_URL}/auth/login
# Las peticiones autenticadas envían la cookie httpOnly con credentials: include
VITE_API_URL=http://localhost:3001
